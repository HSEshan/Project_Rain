# Mirrors setup.py `install_requires`. Services install the package itself
# (`pip install ./libs`, or `./libs[db]` for the two that use Postgres), so
# this file is only for working on libs standalone.
protobuf==6.31.1
grpcio==1.74.0
grpcio-tools==1.74.0
pydantic==2.11.7
pydantic-settings==2.9.1
pydantic_core==2.33.2
structlog==25.4.0
setuptools==80.9.0
orjson==3.11.1

# `db` extra
SQLAlchemy==2.0.43
asyncpg==0.30.0
greenlet==3.2.4
alembic==1.16.4
